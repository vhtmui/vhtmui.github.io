/**
 * @property {boolean} display - indicate sidebar's display.
 * @property {boolean} displayToc - Indicate right sidebar's display.
 */
export const displays = $state({ display: false, displayToc: false });